export type DateTimeFormat = {
  display: string; // eg. 'dd/MM/yyyy HH:mm'
};

// Config default, editable si se requiere
export const DATE_TIME_FORMAT: DateTimeFormat = {
  display: 'dd/MM/yyyy HH:mm',
};
